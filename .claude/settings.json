{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for f in $CLAUDE_FILE_PATHS; do [[ \"$f\" == *.php ]] && rel=\"${f#$PWD/}\" && [[ \"$rel\" == backend/* ]] && ddev exec bash -c \"cd /var/www/html/backend && vendor/bin/phpstan analyse --no-progress '${rel#backend/}'\"; done; exit 0"
          }
        ]
      }
    ]
  }
}
